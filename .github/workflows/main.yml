on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  testing:
    permissions:
      contents: "read"
      id-token: "write"
    name: hack
    runs-on: ubuntu-latest
    steps:
      # use custom action on github.com/navikt/johnny-action
      - name: Run johnny-action
        uses: navikt/johnny-action@v1
        with:
          greeting: "Hello, world!"

